#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
Анализатор карточек клиентов с расширенной функциональностью
Версия 2.0 - Полная переработка с интеграцией БД и напоминаний
"""

import os
import json
import logging
import re
import psycopg2
import psycopg2.extras
from datetime import datetime, timedelta, timezone
from typing import Dict, List, Optional, Any, Tuple
import vertexai
from vertexai.generative_models import GenerativeModel, GenerationConfig, HarmCategory, HarmBlockThreshold
from google.oauth2 import service_account
import time
import threading
from functools import wraps

# Загружаем переменные окружения из .env файла
from dotenv import load_dotenv
load_dotenv()

# Настройка логирования
logging.basicConfig(
    level=logging.INFO,
    format='%(asctime)s - %(levelname)s - %(message)s',
    handlers=[
        logging.FileHandler('client_card_analyzer.log', encoding='utf-8'),
        logging.StreamHandler()
    ]
)

class TimeoutError(Exception):
    """Исключение для тайм-аута"""
    pass

def with_timeout(timeout_seconds):
    """Декоратор для ограничения времени выполнения функции (работает на Windows)"""
    def decorator(func):
        @wraps(func)
        def wrapper(*args, **kwargs):
            result = [None]  # type: List[Any]
            exception = [None]  # type: List[Optional[Exception]]
            
            def target():
                try:
                    result[0] = func(*args, **kwargs)
                except Exception as e:
                    exception[0] = e
            
            thread = threading.Thread(target=target)
            thread.daemon = True
            thread.start()
            thread.join(timeout_seconds)
            
            if thread.is_alive():
                # Поток все еще работает - произошел тайм-аут
                logging.error(f"[TIMEOUT] Функция {func.__name__} превысила лимит времени {timeout_seconds} секунд")
                raise TimeoutError(f"Операция превысила лимит времени {timeout_seconds} секунд")
            
            if exception[0] is not None:
                raise exception[0]
            
            return result[0]
        
        return wrapper
    return decorator

# Константы
PROJECT_ID = "muzvideo2"
LOCATION = "us-central1"
MODEL_NAME = "gemini-2.5-pro"
DATABASE_URL = os.environ.get("DATABASE_URL")

# Настройки таймаута и повторных попыток
GEMINI_TIMEOUT_SECONDS = 180  # 3 минуты максимум на один запрос
MAX_RETRIES = 2  # Максимум 2 повторные попытки
RETRY_DELAY_SECONDS = 30  # Задержка между попытками

# Промпт для стратегического анализа карточки клиента
CARD_ANALYSIS_PROMPT = """
Ты — ИИ-стратег онлайн-школы фортепиано MuzVideo2.ru с глубоким пониманием психологии клиентов и продаж.

ТВОЯ ГЛАВНАЯ ЗАДАЧА: 
1. ТОЧНО обновить карточку клиента по строгим критериям
2. Разработать краткосрочную и долгосрочную стратегию
3. Поставить ОБЯЗАТЕЛЬНОЕ напоминание (каждый клиент должен получить напоминание)

=== ИНФОРМАЦИЯ О ПРОДУКТАХ ШКОЛЫ ===
ОНЛАЙН-ШКОЛА ФОРТЕПИАНО СЕРГЕЯ ФИЛИМОНОВА muzvideo2.ru

О ШКОЛЕ:
- Основана в 2011 году (более 13 лет опыта)
- Более 4500 учеников прошли обучение
- Уникальная система из 12 курсов альтернативного обучения
- Специализация: аккорды, гармония, импровизация, свободная игра
- Авторские методики от Сергея Филимонова
- Куратор с высшим музыкальным образованием
- Дружная атмосфера в чате учеников и онлайн-концерты

ОСНОВНЫЕ КУРСЫ (в порядке изучения):
1. "Виртуозная техника игры за 12 минут в день" - развитие техники, убирает "деревянные пальцы"
2. "Первые шаги" - базовые навыки, ноты, постановка рук, простые аккорды и аккомпанемент (3-6 мес для начинающих)
3. "Игра по нотам с листа" - около 400 произведений, развитие координации рук, ритм и темп
4. "Аккорд Мастер" - простые, сложные и джазовые аккорды, буквенные обозначения, авторский метод формул
5. "Мастер Гармонии" - аккордовые взаимоотношения через интервалы
6. "Импровизация с нуля" - преодоление барьера импровизации, пошаговые упражнения для тех, кто уже умеет играть двумя руками уверенно и знает аккорды.
7. "Мастер Аранжировки" - создание фортепианных аранжировок, требует все предыдущие навыки

ДОПОЛНИТЕЛЬНЫЕ КУРСЫ:
8. "Ноты прочь! Играю что хочу!" - свободная игра без нот
9. "Блюз Мастер" - игра блюза на фортепиано (импровизация, риффы, аккомпанемент, 12-тактовый блюз, бибоп)
10. "Баллад Мастер" - игра на педали в современном и ритмичном стиле (рок-баллады). Ритмика, аккорды, гармония, импровизация в балладном стиле.
11. "10 шагов к музыкальной свободе" - приложение к курсу "Ноты прочь! Играю что хочу!"
12. "Качественная запись фортепиано" - как записывать электронное пианино через шнур aux или midi-кабель.

⚠️ КРИТИЧЕСКИ ВАЖНО: ТОЛЬКО СУЩЕСТВУЮЩИЕ ПРОДУКТЫ!
Используй ТОЛЬКО курсы из списка выше. НЕ придумывай несуществующие мастер-классы, курсы или продукты. Все названия должны точно соответствовать реальному каталогу школы.

НАБОРЫ КУРСОВ:
1. "Всё включено" (12 курсов)
2. "6 шагов" (курсы 1-7)
3. "Начинающий+" (курсы 1-4)
4. "Аккорд-Мастер"+"Мастер Гармонии"
5. "Аккорд-Мастер"+"Блюз-Мастер"+"Баллад мастер"

МАСТЕР-КЛАССЫ:
1. МК по импровизации в джазовом стиле на основе композиции Чарли Паркера "My Little Suede Shoes"
2. МК по сочинению музыки на фортепиано (демо работы композитора по сочинению 5 произведений, в т.ч. одной песни)
3. Мини-курс по нотному редактору MuseScore

ЦЕЛЕВЫЕ АУДИТОРИИ: женщины 35+ (70%), мужчины 35+ (30%). Детей не обучаем.
1. Начинающие или "всё забыл" - утратившие навыки
2. Продолжающие - играют уверенно, но не могут играть свободно.
3. С музыкальным образованием по фортепиано (1 - хотят импровизировать, сочинять; 2 - хотят улучшить акк-т; 3 - хотят получить методики для обучения взрослых)
4. С музыкальным образованием по другим инструментам

ЦЕЛИ ОБУЧЕНИЯ:
- Свободная игра (подбор, аккомпанемент, импровизация, сочинение)
- Игра только по нотам (классика, современные произведения)
- Комбинированное обучение (ноты + свободная игра)

ПРЕИМУЩЕСТВА:
- Авторские методики, отточенные за с 2011 года
- Личная поддержка от автора школы
- Гибкие условия продления (1000 руб/год)
- Платформа работает без VPN
- Возможность рассрочки от банков
- Система скидок для клиентов

=== СИСТЕМА СКИДОК И ПРОДАЖНЫЕ ИНСТРУМЕНТЫ ===

💰 ПРАВИЛА ПРЕДОСТАВЛЕНИЯ СКИДОК:

**ПОЛНОМОЧИЯ ИИ-КОММУНИКАТОРА, ДЛЯ КОТОРОГО ТЫ ПИШЕШЬ НАПОМИНАНИЕ:**
- Небольшие скидки (до 15%) - может предложить самостоятельно "от себя"
- Большие скидки (до 50%) - только с согласия автора школы
- давать пакетные скидки
- давать промокоды для существующих клиентов
- давать ссылки на подробное описание курсов, на отзывы, на личные контакты Сергея (автора школы)
- Дать бесплатно урок из платного курса для демонстрации (с согласия автора школы)
- Дать демонстрационное видео по обучающей площадке (если клиент сомневается) по конкретному курсу
- Стиль торговли: как в дорогих автосалонах, не как на базаре

**ДОПОЛНИТЕЛЬНЫЕ БОНУСЫ:**
- 1-3 месяца дополнительного доступа бесплатно (для тех, кто не может сейчас начать)
- Один из мастер-классов онлайн-школы бесплатно к покупке набора курсов

**СКИДКИ ДЛЯ СУЩЕСТВУЮЩИХ КЛИЕНТОВ:**
1. Повторные покупки: 5% скидка за лояльность (промокод "klient")
2. Скидка за отзыв: 15% на отдельные курсы или 5000 руб на наборы (после публикации отзыва ВК)

**ПАКЕТНЫЕ СКИДКИ:**
- 2 курса = скидка 15% (агент-коммуникатор имеет ссылки на покупку из раздела "курсы со скидкой 15%")
- 3-4 курса = скидка 30% (коммуникатор рассчитывает стоимость и формирует персональную ссылку)
- 5+ курсов = скидка 50% (но лучше предлагать набор "всё включено" как более выгодный)

**ВРЕМЕННЫЕ СКИДКИ:**
- Скидка 5000 руб на наборы при оплате в течение недели.
- Готовые ссылки на наборы со скидкой 5000 руб: "Всё включено" - 54990р, "6 шагов" - 44990р, "Начинающий+" - 24990р

💳 БАНКОВСКАЯ РАССРОЧКА:

**ДОСТУПНЫЕ СИСТЕМЫ:**
- Т-Банк (Тинькофф)
- Сбербанк
- Яндекс.сплит
- всегда.да
- долями
- подели (Альфа-банк)

**ПРЕИМУЩЕСТВА РАССРОЧКИ:**
- Автоматическое списание ежемесячных платежей
- отсутвие первого платежа
- Оформление онлайн через ссылку оплаты
- Удобство для клиента

**НЕУДОБСТВА РАССРОЧКИ:**
- Нечастое одобрение (мнительные клиенты часто реагируют на отказ банка как "не судьба")

**ВАЖНО:** Предлагаем ТОЛЬКО банковскую рассрочку, НЕ личную рассрочку от школы!

🎯 ДОПОЛНИТЕЛЬНЫЕ ПРОДАЖНЫЕ ИНСТРУМЕНТЫ:

**КОНСУЛЬТАЦИИ С АВТОРОМ:**
- Созвон в Zoom с Сергеем для дорогих наборов ("всё включено", "6 шагов", "Начинающий+")
- Цели: ответы "из первых уст", снижение напряжённости, уменьшение отказов, доверие
- Длительность: 20-40 минут
- Альтернативы Zoom: Google Meet, Яндекс, Телеграм, WhatsApp

**РАБОТА С ВОЗРАЖЕНИЯМИ:**
- "Дорого" → предлагать скидки, торговаться, созвон с автором
- "Нет времени/занят/подумаю" → метод рефрейминга
- Стандартные возражения → конструктивное обсуждение

**КОНТАКТЫ АВТОРА:**
- ВКонтакте: vk.com/sergey_piano
- WhatsApp: +79609921955
- Telegram: t.me/muzvideo2

💡 СТРАТЕГИИ ВОЗОБНОВЛЕНИЯ ДИАЛОГА:

**ОСНОВНЫЕ ПРИЧИНЫ ДЛЯ КОНТАКТА:**
1. Улучшение оффера (лучшие условия)
2. Предложение скидки (персональной или временной)
3. Отработка возражений (решение проблем клиента)
4. Напоминание об оплате (мягкое, без давления)
5. Предложение рассрочки (снижение финансового барьера)
6. Выяснение причин молчания (исследование проблем)
7. Информация о новых возможностях (курсы, мастер-классы)
8. Предложение консультации с автором (повышение доверия)

**ТАКТИКИ ВОЗОБНОВЛЕНИЯ:**
- Персональные предложения на основе истории клиента
- Создание ощущения упущенной возможности (ограниченные по времени скидки)
- Предложение решения конкретных проблем клиента
- Мягкое напоминание о предыдущих договорённостях
- Информирование о новых преимуществах или изменениях
- Возобновление обсуждения покупки курсов после долгого перерыва
- Возврат к теме обучения на курсах, если разговор ушёл в другое русло

=== ДАННЫЕ КЛИЕНТА ===
{client_data}

=== КРИТИЧЕСКИ ВАЖНЫЕ ПРАВИЛА АНАЛИЗА ===

⚠️ СТРОГИЕ КРИТЕРИИ АНАЛИЗА:
- При анализе квалификации клиента используй ТОЧНЫЕ критерии ниже
- ЗАЩИЩАЙ критически важные этапы воронки от понижения
- НЕ теряй информацию из существующего профиля - только дополняй
- Используй иерархию этапов воронки для правильных переходов

1. **КВАЛИФИКАЦИЯ ЛИДА** (lead_qualification) - СТРОГИЕ КРИТЕРИИ:

ХОЛОДНЫЙ КЛИЕНТ:
- Первый контакт без упоминания платного обучения
- Запрашивает бесплатные уроки или лид-магниты ("Хочу урок по нисходящей гармонии")
- Просто пишет смайлики, благодарности, общие фразы ("Спасибо", "👍", "Привет")
- Рассказывает о себе, но НЕ спрашивает об обучении
- Любые сообщения БЕЗ упоминания: курсов, цен, платного обучения
- ВАЖНО: "урок" = НЕ интерес к платному обучению, а запрос бесплатного контента

ТЁПЛЫЙ КЛИЕНТ:
- Впервые спрашивает о платном обучении ("хочу научиться играть", "сколько стоят курсы")
- Интересуется обучением, но НЕ называет конкретных курсов
- Общие вопросы о процессе ("Как проходит обучение?")

ГОРЯЧИЙ КЛИЕНТ:
- Называет конкретные курсы ("Первые шаги", "Аккорд-Мастер")
- Спрашивает о скидках на конкретные продукты
- Готовность к покупке ("созрел", "решил заняться обучением")
- Упоминает наборы ("самый полный набор", "СТАНДАРТ", "ПРЕМИУМ")

КЛИЕНТ:
- Уже совершил покупку, нужна поддержка и допродажи

2. **ЭТАП ВОРОНКИ** (funnel_stage) - СТРОГАЯ ИЕРАРХИЯ:

Иерархия этапов (по приоритету):
0. не применимо - лид-магниты, бесплатные уроки, смайлики
1. предложение по продуктам ещё не сделано - заинтересовался, но цены не названы
2. сделано предложение по продуктам - получил конкретный продукт + цену
3. сделано новое предложение - новое предложение после покупки/отказа  
4. клиент думает - уточняющие вопросы после получения цены
5. у клиента есть возражения - "дорого", "некогда", перестал отвечать
6. отказ от покупки - чёткий явный отказ
7. решение принято (ожидаем оплату) - сообщил КОГДА заплатит
8. покупка совершена - прислал чек или сообщил об оплате

⚠️ ЗАЩИТА КРИТИЧЕСКИХ ЭТАПОВ:
- НЕ понижай этапы 4,7,8 без веских оснований
- После "покупка совершена" или "отказ" можно ставить "сделано новое предложение"

3. **СТРАТЕГИЧЕСКИЙ АНАЛИЗ** - внутренний анализ:

<internal_analysis>
Ты ОБЯЗАН провести внутренний анализ:
1. Психологический портрет клиента и эмоциональное состояние
2. Оценка комфорта в диалоге (1-10), причины дискомфорта и действия для улучшения
3. Стадия воронки и соответствие нашего подхода
4. Возможные вопросы для продвижения к цели
5. Краткосрочная стратегия: цель на 1-2 взаимодействия
6. Долгосрочная стратегия: видение клиента через год, повышение LTV
7. Следующий шаг: конкретное действие в следующем сообщении
</internal_analysis>

⚠️ КРИТИЧЕСКИ ВАЖНО: РОЛЬ СТРАТЕГА
Ты - СТРАТЕГ, НЕ коммуникатор! Твоя задача:
- АНАЛИЗИРОВАТЬ клиента и ситуацию
- ОПРЕДЕЛЯТЬ стратегическое НАПРАВЛЕНИЕ (продавать/дарить/поддерживать/выяснять)
- УКАЗЫВАТЬ психологический подход
- ОБОЗНАЧАТЬ цель и желаемый результат

🚫 ЧЕГО НЕ ДОЛЖЕН ДЕЛАТЬ СТРАТЕГ:
- НЕ пиши конкретные тексты сообщений (это задача коммуникатора)
- НЕ называй конкретные продукты в качестве подарка (коммуникатор сам выберет из каталога)
- НЕ расписывай детальные сценарии диалога
- НЕ придумывай несуществующие продукты

✅ ПРАВИЛЬНЫЕ ПРИМЕРЫ СТРАТЕГИЧЕСКИХ НАПРАВЛЕНИЙ:
- "Направление: подарок без обязательств. Психология: снятие напряжения, возвращение доверия"
- "Направление: мягкое выяснение ситуации. Подход: без давления, как друг интересуется успехами"
- "Направление: поддержка и допродажи. Фокус: на успехах в обучении, дополнительные возможности"

4. **ОБЯЗАТЕЛЬНЫЕ НАПОМИНАНИЯ** - каждый клиент должен получить напоминание:

⚠️ КРИТИЧЕСКИ ВАЖНО: ПРОВЕРКА СТАТУСА ПОКУПКИ
Перед разработкой стратегии "возврата потерянного клиента" ОБЯЗАТЕЛЬНО учти:
- ты видишь ТОЛЬКО диалог ВКонтакте, а также карточку клиента, но не всегда в ней данные о покупке синхронизированы с БД покупок.
- Если клиент "пропал" после обещания оплаты, это НЕ означает, что он не заплатил
- Клиенты часто покупают курсы, но не сообщают об этом в чат ВК

🔍 КРИТИЧЕСКИ ВАЖНО: ДАННЫЕ О ПОКУПКАХ ИМЕЮТ ПРИОРИТЕТ!
- В данных клиента есть два раздела: "purchases" (по conv_id) и "email_purchases" (найденные по email из переписки)
- Если в любом из разделов есть покупки - клиент УЖЕ является покупателем
- Данные о покупках более достоверны, чем информация из диалога
- Если есть покупки, подтверждение в диалоге НЕ требуется
- При наличии покупок клиент автоматически переводится в статус "клиент" с этапом воронки "покупка совершена" или "сделано новое предложение" (для допродаж)

🔍 ОБЯЗАТЕЛЬНАЯ СТРАТЕГИЯ ПРОВЕРКИ:
В случаях когда:
- Статус "ожидаем оплату" 
- Долгое отсутствие клиента после обещания купить
- Разговор был давно (более недели назад)

ПЕРВЫМ ШАГОМ должно быть:
1. Мягкое выяснение текущего статуса ("Как дела с курсами?", "Получилось ли начать обучение?")
2. Проверка, не купил ли клиент уже что-то
3. ТОЛЬКО после подтверждения, что покупка НЕ состоялась - стратегия "возврата клиента"

ПРИМЕР ПРАВИЛЬНОГО ПОДХОДА:
Вместо: "Стратегия возврата потерянного горячего клиента"
Правильно: "Сначала выясняем статус покупки. Если не купил - стратегия возврата"

АНАЛИЗ ОПТИМАЛЬНОГО ВРЕМЕНИ:
- Проанализируй timestamps сообщений клиента из dialogue_history
- Обрати внимание на СВЕЖИЕ данные (приоритет последним дням/неделям)
- Определи, когда клиент отвечает развёрнуто vs односложно
- Учти его общий паттерн: если всегда односложен - это норма, если вчера был односложен, а обычно развёрнут - вчера было неудобно
- Верни ТОЧНЫЙ UTC час (0-23) для оптимального контакта

ВРЕМЯ НАПОМИНАНИЯ - КРИТИЧЕСКИ ВАЖНАЯ ЛОГИКА С АВТОМАТИЧЕСКОЙ ОЦЕНКОЙ ПОТЕНЦИАЛА:

🧠 АВТОМАТИЧЕСКАЯ ОЦЕНКА ПОТЕНЦИАЛА ПОКУПКИ В БЛИЖАЙШИЕ 1-2 ГОДА:
Перед определением времени напоминания АВТОМАТИЧЕСКИ проанализируй поведение клиента:

**ПРИЗНАКИ НИЗКОГО ПОТЕНЦИАЛА ПОКУПКИ (отсрочка 6-12 месяцев):**
- Клиент использует коммуникатора НЕ по назначению: обсуждает музыку в общем, личные проблемы, философию
- Постоянно уходит от темы покупки курсов на посторонние темы
- Показывает признаки "халявщика": просит только бесплатное, избегает разговоров о платном обучении
- Ведёт себя как "вампир времени": длинные беседы без движения к покупке
- Явно заявляет о финансовых трудностях или нежелании тратить деньги в ближайшие 1-2 года
- История показывает паттерн: много общения, ноль покупок за длительный период
- Коммуникатор УЖЕ делал 3+ попытки активации (НЕ массовые рассылки, а ПЕРСОНАЛЬНЫЕ сообщения)
- Клиент НЕ отвечает на персональные попытки возобновления контакта
- Последняя активность более 6 месяцев назад без признаков заинтересованности
- Клиент читает сообщения ("прочитано"), но не отвечает на персональные предложения

**ДЛЯ НИЗКОПОТЕНЦИАЛЬНЫХ КЛИЕНТОВ → АВТОМАТИЧЕСКИ СТАВЬ ДОЛГОСРОЧНУЮ ОТСРОЧКУ:**
- contact_in_days: 180-365 (6-12 месяцев)
- НЕ объясняй причину клиенту - просто ставь отсрочку

**ДЛЯ СПЯЩИХ КЛИЕНТОВ С МНОЖЕСТВЕННЫМИ ПОПЫТКАМИ → АВТОМАТИЧЕСКИ СТАВЬ ОТСРОЧКУ:**
- contact_in_days: 180-270 (6-9 месяцев)
- НЕ объясняй причину клиенту - просто ставь отсрочку

**СТАНДАРТНАЯ ЛОГИКА ДЛЯ ПЕРСПЕКТИВНЫХ КЛИЕНТОВ:**
⚡ ПО УМОЛЧАНИЮ: СЕГОДНЯ В УДОБНОЕ ДЛЯ КЛИЕНТА ВРЕМЯ
- Если нет веских причин для отсрочки → ставь напоминание на СЕГОДНЯ
- Определи оптимальный час на основе активности клиента из dialogue_history  
- Если диалог был 5 лет назад и клиент был активен в 15:00 → ставь на СЕГОДНЯ в 15:00
- Если диалог был вчера и клиент отвечал вечером → ставь на СЕГОДНЯ вечером
- Если коммуникатор писал клиенту и пытался восстановить контакт (без ответа со стороны клиента), то ставить ЧЕРЕЗ НЕДЕЛЮ
- Если попытки возобновления контакта были неоднократными со стороны бота (не рассылки), то ставь через 3 недели.

⏰ ОТСРОЧКА ТОЛЬКО ПРИ ВЕСКИХ ПРИЧИНАХ:
- Клиент сообщил, что болеет/в отпуске/занят → отсрочка на указанный период
- Клиент просил перезвонить через определённое время → точно в указанное время
- Есть договорённость о конкретной дате контакта → в договорённую дату
- Клиент явно попросил не беспокоить какое-то время → учесть просьбу

🚫 НЕ ЯВЛЯЕТСЯ ПРИЧИНОЙ ДЛЯ ОТСРОЧКИ (для перспективных клиентов):
- "Давно не общались" - НЕ причина откладывать
- "Нужно дать время подумать" - только если клиент сам просил время
- "Не хочу быть навязчивым" - НЕ причина, клиент уже в базе
- Любые общие соображения без конкретных обстоятельств

ПРАВИЛА ОПРЕДЕЛЕНИЯ contact_in_days НА ОСНОВЕ АВТОМАТИЧЕСКОГО АНАЛИЗА:
- 0 дней = СЕГОДНЯ (для перспективных клиентов)
- 1-7 дней = только при наличии конкретной причины отсрочки
- 180-365 дней = автоматически для низкопотенциальных клиентов (БЕЗ объяснений)

- Цель: провоцировать на разговор, двигать по воронке, проверять договорённости
- Причина: ВСЕГДА весомая и понятная клиенту

5. **ОБНОВЛЕНИЕ ПРОФИЛЯ**:
   client_level: уровни игры (новичок/начинающий/продолжающий/продвинутый)
   learning_goals: цели обучения (импровизация/классика/аккорды/восстановление навыков)
   client_pains: боли (нет времени/сомнения/финансы/нет инструмента/страх неудачи)
   email: ВСЕ найденные email-адреса
   purchased_products: упомянутые купленные курсы
   client_activity: активен (2+ сообщения) / пассивен (0-1 сообщение)

6. **ФОРМАТ ОТВЕТА JSON** (строго соблюдай структуру):

```json
{{
  "profile_updates": {{
    "lead_qualification": ["тёплый"],
    "funnel_stage": "клиент думает",
    "client_level": ["начинающий"],
    "learning_goals": ["импровизация"],
    "client_pains": ["нет времени"],
    "email": ["user@example.com"],
    "purchased_products": ["Первые шаги"],
    "client_activity": "активен"
  }},
  "strategic_analysis": {{
    "psychological_profile": "Тип личности и мотивация",
    "comfort_level": 8,
    "comfort_issues": "Причины дискомфорта если есть",
    "short_term_strategy": "Направление: мягкое выяснение статуса. Подход: дружеский, без давления",
    "long_term_strategy": "Видение через год: развитие отношений, повышение LTV через допродажи",
    "next_action": "Направление: проявить интерес к успехам в обучении. Психология: поддержка и позитивное подкрепление"
  }},
  "mandatory_reminder": {{
    "contact_in_days": 0,
    "optimal_utc_hour": 19,
    "activity_analysis": "На основе timestamps: клиент активен вечером 18-20 UTC, последние сообщения в 19:30. Отсутствуют обстоятельства для отсрочки, поэтому ставим на сегодня",
    "reminder_context": "Дай ИИ-агенту коммуникатору чёткое стратегическое НАПРАВЛЕНИЕ, НЕ конкретную реализацию. КРИТИЧЕСКИ ВАЖНО: Обязательно учитывай данные о покупках - они приоритетны перед информацией из диалогов. Если есть покупки (в purchases или email_purchases) - клиент уже покупатель, направление: поддержка, интерес к успехам, допродажи. НЕ называй конкретные продукты/мастер-классы - коммуникатор сам выберет из каталога. НЕ пиши тексты сообщений - это роль коммуникатора. Если клиент 'пропал' после обещания оплаты НО ПОКУПОК НЕТ, сначала МЯГКО выясни статус. Основные стратегические направления: продвижение по воронке, улучшение оффера, персональные скидки, отработка возражений, напоминание об оплате, банковская рассрочка, выяснение причин молчания, подарок без обязательств. Опиши психологический подход к ДАННОМУ клиенту, укажи желаемый результат и основные риски."
  }}
}}
```

⚠️ КРИТИЧЕСКИ ВАЖНО:
- ОБЯЗАТЕЛЬНО проведи оценку потенциала покупки ПЕРЕД определением времени напоминания
- Используй ТОЧНЫЕ критерии выше для квалификации и воронки
- Для низкопотенциальных клиентов ставь отсрочку 6-12 месяцев
- Для спящих клиентов с множественными попытками активации ставь отсрочку 6-9 месяцев
- НЕ понижай этапы воронки без веских оснований 
- ОБЯЗАТЕЛЬНО ставь напоминание каждому клиенту (но с правильной отсрочкой)
- Проводи глубокий внутренний анализ клиента включая потенциал покупки
- Дополняй профиль, НЕ заменяй существующую информацию

⚡ ОБЯЗАТЕЛЬНО: Верни ТОЛЬКО валидный JSON объект, начинающийся с {{ и заканчивающийся }}, без дополнительного текста!
"""

class ClientCardAnalyzer:
    """Анализатор карточек клиентов с интеграцией БД"""
    
    def __init__(self):
        """Инициализация анализатора"""
        self.model = None
        self._initialize_vertex_ai()
    
    def _initialize_vertex_ai(self):
        """Инициализация Vertex AI с детальными проверками"""
        try:
            # Проверяем переменные окружения
            credentials_path = os.environ.get("GOOGLE_APPLICATION_CREDENTIALS")
            logging.info(f"[VERTEX_AI] Проверяем путь к credentials: {credentials_path}")
            
            if not credentials_path:
                raise RuntimeError("GOOGLE_APPLICATION_CREDENTIALS не установлена")
            
            credentials_path = credentials_path.strip(' "')
            logging.info(f"[VERTEX_AI] Очищенный путь: {credentials_path}")
            
            if not os.path.exists(credentials_path):
                raise RuntimeError(f"Файл учетных данных не найден: {credentials_path}")
            
            logging.info(f"[VERTEX_AI] Загружаем credentials из файла...")
            credentials = service_account.Credentials.from_service_account_file(credentials_path)
            logging.info(f"[VERTEX_AI] Credentials загружены успешно")
            
            logging.info(f"[VERTEX_AI] Инициализируем Vertex AI: project={PROJECT_ID}, location={LOCATION}")
            vertexai.init(project=PROJECT_ID, location=LOCATION, credentials=credentials)
            
            logging.info(f"[VERTEX_AI] Создаём модель: {MODEL_NAME}")
            
            # Настройки генерации
            generation_config = GenerationConfig(
                temperature=0.1,  # Низкая температура для стабильности
                top_p=0.8,
                top_k=40,
                max_output_tokens=8192,  # Достаточно для JSON
            )
            
            # Настройки безопасности - отключаем все фильтры
            safety_settings = {
                HarmCategory.HARM_CATEGORY_HATE_SPEECH: HarmBlockThreshold.BLOCK_NONE,
                HarmCategory.HARM_CATEGORY_DANGEROUS_CONTENT: HarmBlockThreshold.BLOCK_NONE,
                HarmCategory.HARM_CATEGORY_SEXUALLY_EXPLICIT: HarmBlockThreshold.BLOCK_NONE,
                HarmCategory.HARM_CATEGORY_HARASSMENT: HarmBlockThreshold.BLOCK_NONE,
            }
            
            self.model = GenerativeModel(
                MODEL_NAME,
                generation_config=generation_config,
                safety_settings=safety_settings
            )
            
            # Проверяем, что модель создана
            if self.model is None:
                raise RuntimeError("Модель не создана")
            
            logging.info("Vertex AI успешно инициализирован")
            
        except Exception as e:
            logging.error(f"Ошибка инициализации Vertex AI: {e}")
            logging.error(f"Тип ошибки: {type(e)}")
            raise
    
    def get_db_connection(self) -> psycopg2.extensions.connection:
        """Получение соединения с БД"""
        if not DATABASE_URL:
            raise ValueError("DATABASE_URL не настроен")
        
        try:
            conn = psycopg2.connect(DATABASE_URL)
            return conn
        except psycopg2.Error as e:
            logging.error(f"Ошибка подключения к БД: {e}")
            raise
    
    def _extract_emails_from_dialogue(self, dialogue_history: List[Dict]) -> List[str]:
        """Извлекает все email адреса из истории диалога"""
        import re
        emails = set()
        email_pattern = r'\b[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Z|a-z]{2,}\b'
        
        for message in dialogue_history:
            text = message.get('message', '')
            found_emails = re.findall(email_pattern, text)
            emails.update(found_emails)
        
        return list(emails)
    
    def _find_purchases_by_email(self, emails: List[str]) -> List[Dict[str, Any]]:
        """Поиск покупок по email адресам"""
        if not emails:
            return []
            
        purchases = []
        try:
            with self.get_db_connection() as conn:
                with conn.cursor(cursor_factory=psycopg2.extras.DictCursor) as cur:
                    # Ищем покупки по email
                    for email in emails:
                        cur.execute("""
                            SELECT product_name, purchase_date, amount, email, conv_id
                            FROM client_purchases 
                            WHERE LOWER(email) = LOWER(%s)
                            ORDER BY purchase_date DESC
                        """, (email,))
                        email_purchases = cur.fetchall()
                        
                        for purchase in email_purchases:
                            purchase_dict = dict(purchase)
                            purchase_dict['found_via_email'] = email  # Отмечаем, как нашли
                            purchases.append(purchase_dict)
                            
        except Exception as e:
            logging.error(f"Ошибка поиска покупок по email: {e}")
            
        return purchases
    
    def _update_profile_email(self, conv_id: int, emails: List[str]) -> None:
        """Обновляет email в профиле клиента"""
        if not emails:
            return
            
        try:
            with self.get_db_connection() as conn:
                with conn.cursor() as cur:
                    # Проверяем, есть ли профиль
                    cur.execute("SELECT email FROM user_profiles WHERE conv_id = %s", (conv_id,))
                    result = cur.fetchone()
                    
                    if result:
                        existing_emails = result[0] if result[0] else []
                        # Объединяем существующие и новые email
                        all_emails = list(set(existing_emails + emails))
                        
                        cur.execute("""
                            UPDATE user_profiles 
                            SET email = %s, updated_at = CURRENT_TIMESTAMP
                            WHERE conv_id = %s
                        """, (all_emails, conv_id))
                        
                        logging.info(f"Обновлен email для клиента {conv_id}: {all_emails}")
                    else:
                        logging.warning(f"Профиль клиента {conv_id} не найден для обновления email")
                        
        except Exception as e:
            logging.error(f"Ошибка обновления email в профиле: {e}")
    
    def _link_purchases_to_client(self, conv_id: int, email_purchases: List[Dict]) -> None:
        """Связывает покупки с клиентом по conv_id"""
        if not email_purchases:
            return
            
        try:
            with self.get_db_connection() as conn:
                with conn.cursor() as cur:
                    for purchase in email_purchases:
                        email = purchase.get('found_via_email')
                        if email and purchase.get('conv_id') != conv_id:
                            # Обновляем conv_id в таблице покупок
                            cur.execute("""
                                UPDATE client_purchases 
                                SET conv_id = %s
                                WHERE email = %s AND conv_id IS DISTINCT FROM %s
                            """, (conv_id, email, conv_id))
                            
                            rows_updated = cur.rowcount
                            if rows_updated > 0:
                                logging.info(f"Обновлено {rows_updated} покупок для клиента {conv_id} по email {email}")
                            
        except Exception as e:
            logging.error(f"Ошибка связывания покупок с клиентом: {e}")

    def load_client_data_from_db(self, conv_id: int) -> Dict[str, Any]:
        """Загрузка данных клиента с автоматическим поиском email и сопоставлением покупок"""
        client_data = {
            "conv_id": conv_id,
            "profile": {},
            "dialogue_history": [],
            "purchases": [],
            "email_purchases": [],  # Покупки найденные по email
            "active_reminders": []
        }
        
        try:
            with self.get_db_connection() as conn:
                with conn.cursor(cursor_factory=psycopg2.extras.DictCursor) as cur:
                    # Профиль клиента
                    cur.execute("""
                        SELECT * FROM user_profiles WHERE conv_id = %s
                    """, (conv_id,))
                    profile = cur.fetchone()
                    if profile:
                        client_data["profile"] = dict(profile)
                    
                    # История диалогов (последние 20 сообщений)
                    cur.execute("""
                        SELECT role, message, created_at 
                        FROM dialogues 
                        WHERE conv_id = %s 
                        ORDER BY created_at DESC 
                        LIMIT 20
                    """, (conv_id,))
                    messages = cur.fetchall()
                    client_data["dialogue_history"] = [dict(msg) for msg in reversed(messages)]
                    
                    # Покупки по conv_id
                    cur.execute("""
                        SELECT product_name, purchase_date, amount, email
                        FROM client_purchases 
                        WHERE conv_id = %s
                        ORDER BY purchase_date DESC
                    """, (conv_id,))
                    purchases = cur.fetchall()
                    client_data["purchases"] = [dict(purchase) for purchase in purchases]
                    
                    # Активные напоминания
                    cur.execute("""
                        SELECT reminder_datetime, reminder_context_summary, created_at
                        FROM reminders 
                        WHERE conv_id = %s AND status = 'active'
                        ORDER BY reminder_datetime
                    """, (conv_id,))
                    reminders = cur.fetchall()
                    client_data["active_reminders"] = [dict(reminder) for reminder in reminders]
            
            # КРИТИЧЕСКИ ВАЖНО: Поиск email в переписке и сопоставление с покупками
            logging.info(f"Поиск email в переписке клиента {conv_id}")
            
            # 1. Извлекаем email из переписки
            emails_in_dialogue = self._extract_emails_from_dialogue(client_data["dialogue_history"])
            
            if emails_in_dialogue:
                logging.info(f"Найдены email в переписке: {emails_in_dialogue}")
                
                # 2. Ищем покупки по этим email
                email_purchases = self._find_purchases_by_email(emails_in_dialogue)
                
                if email_purchases:
                    logging.info(f"Найдено {len(email_purchases)} покупок по email для клиента {conv_id}")
                    client_data["email_purchases"] = email_purchases
                    
                    # 3. Обновляем профиль клиента (добавляем email)
                    self._update_profile_email(conv_id, emails_in_dialogue)
                    
                    # 4. Связываем покупки с этим клиентом
                    self._link_purchases_to_client(conv_id, email_purchases)
                    
                    # 5. Перезагружаем покупки по conv_id (теперь они должны быть связаны)
                    with self.get_db_connection() as conn:
                        with conn.cursor(cursor_factory=psycopg2.extras.DictCursor) as cur:
                            cur.execute("""
                                SELECT product_name, purchase_date, amount, email
                                FROM client_purchases 
                                WHERE conv_id = %s
                                ORDER BY purchase_date DESC
                            """, (conv_id,))
                            updated_purchases = cur.fetchall()
                            client_data["purchases"] = [dict(purchase) for purchase in updated_purchases]
                    
                    logging.info(f"Обновлены покупки клиента {conv_id}: {len(client_data['purchases'])} покупок")
                else:
                    logging.info(f"Покупок по email {emails_in_dialogue} не найдено")
            else:
                logging.info(f"Email в переписке клиента {conv_id} не найден")
            
            logging.info(f"Данные клиента {conv_id} загружены: профиль={len(client_data['profile'])>0}, диалоги={len(client_data['dialogue_history'])}, покупки={len(client_data['purchases'])}, email_покупки={len(client_data['email_purchases'])}")
            return client_data
            
        except Exception as e:
            logging.error(f"Ошибка загрузки данных клиента {conv_id}: {e}")
            raise
    
    @with_timeout(GEMINI_TIMEOUT_SECONDS)
    def _call_gemini_with_timeout(self, prompt: str):
        """Вызов Gemini API с тайм-аутом"""
        if self.model is None:
            raise RuntimeError("Model not initialized")
        return self.model.generate_content(prompt)
    
    def analyze_client_card(self, client_data: Dict[str, Any]) -> Dict[str, Any]:
        """Анализ карточки клиента с помощью AI с максимально надёжным парсингом"""
        response_text = ""  # Инициализируем переменную
        conv_id = client_data.get('conv_id', 'UNKNOWN')
        
        logging.info(f"[ANALYZE] Начинаем анализ клиента {conv_id}")
        
        try:
            # Подготовка данных для промпта
            logging.info(f"[ANALYZE] Подготавливаем данные для промпта...")
            client_data_str = json.dumps(client_data, ensure_ascii=False, indent=2, default=str)
            logging.info(f"[ANALYZE] Данные клиента сериализованы, размер: {len(client_data_str)} символов")
            
            # Формирование промпта
            logging.info(f"[ANALYZE] Формируем промпт...")
            prompt = CARD_ANALYSIS_PROMPT.format(client_data=client_data_str)
            logging.info(f"[ANALYZE] Промпт сформирован, размер: {len(prompt)} символов")
            
            # Проверяем, что модель инициализирована
            logging.info(f"[ANALYZE] Проверяем модель...")
            if self.model is None:
                raise RuntimeError("Model not initialized")
            logging.info(f"[ANALYZE] Модель готова: {type(self.model)}")
            
            # Запрос к AI с тайм-аутом и повторными попытками
            for attempt in range(MAX_RETRIES + 1):
                try:
                    logging.info(f"Отправка запроса к Gemini для анализа клиента {conv_id} (попытка {attempt + 1}/{MAX_RETRIES + 1})")
                    logging.info(f"[DEBUG] Длина промпта: {len(prompt)} символов")
                    logging.info(f"[DEBUG] Первые 200 символов промпта: {prompt[:200]}")
                    logging.info(f"[TIMEOUT] Используем тайм-аут {GEMINI_TIMEOUT_SECONDS} секунд")
                    
                    # Вызов с тайм-аутом
                    response = self._call_gemini_with_timeout(prompt)
                    logging.info(f"[GEMINI] Ответ получен успешно")
                    
                    # Проверяем, что response не None
                    if response is None:
                        raise RuntimeError("Response is None")
                    
                    # Проверяем, есть ли text в response
                    if not hasattr(response, 'text') or response.text is None:
                        logging.error(f"[GEMINI] Response не содержит text: {response}")
                        raise RuntimeError("Response не содержит text")
                    
                    response_text = response.text.strip()
                    logging.info(f"[GEMINI] Получен ответ длиной {len(response_text)} символов")
                    logging.info(f"[GEMINI] ===== ПОЛНЫЙ ОТВЕТ ИИ (БЕЗ ОБРЕЗКИ) ===== conv_id:{conv_id} =====")
                    logging.info(f"{response_text}")
                    logging.info(f"[GEMINI] ===== КОНЕЦ ПОЛНОГО ОТВЕТА ИИ ===== conv_id:{conv_id} =====")
                    
                    # Дополнительная проверка качества ответа
                    if len(response_text) < 50:
                        logging.warning(f"[GEMINI_QUALITY] Очень короткий ответ ({len(response_text)} символов) для клиента {conv_id}")
                    if not '{' in response_text or not '}' in response_text:
                        logging.warning(f"[GEMINI_QUALITY] Ответ не содержит JSON структуру для клиента {conv_id}")
                    
                    # Успешное получение ответа - выходим из цикла
                    break
                    
                except (TimeoutError, Exception) as api_error:
                    logging.error(f"[GEMINI API ERROR] Попытка {attempt + 1}: {api_error}")
                    logging.error(f"[GEMINI API ERROR] Тип ошибки: {type(api_error)}")
                    
                    if attempt < MAX_RETRIES:
                        logging.info(f"[RETRY] Ожидаем {RETRY_DELAY_SECONDS} секунд перед следующей попыткой...")
                        time.sleep(RETRY_DELAY_SECONDS)
                        continue
                    else:
                        logging.error(f"[GEMINI FAILED] Все попытки исчерпаны. Последняя ошибка: {api_error}")
                        raise RuntimeError(f"Ошибка вызова Gemini API после {MAX_RETRIES + 1} попыток: {api_error}")
            
            # СУПЕР-НАДЁЖНОЕ извлечение JSON
            analysis_result = self._extract_json_from_response(response_text, conv_id)
            
            logging.info(f"[OK] Анализ клиента {conv_id} завершен успешно")
            return analysis_result
            
        except Exception as e:
            logging.error(f"[КРИТИЧЕСКАЯ ОШИБКА] Анализ клиента {conv_id}: {e}")
            logging.error(f"[DEBUG] Сырой ответ ИИ: {response_text[:1000]}")
            
            # ЗАЩИТА: возвращаем базовый результат вместо краша
            return self._create_fallback_analysis(client_data)
    
    def _extract_json_from_response(self, response_text: str, conv_id: str) -> Dict[str, Any]:
        """Супер-надёжное извлечение JSON из ответа ИИ"""
        import re
        
        # Стратегия 1: Поиск JSON в markdown блоках
        json_match = re.search(r'```(?:json)?\s*(\{.*?\})\s*```', response_text, re.DOTALL)
        if json_match:
            json_str = json_match.group(1).strip()
            logging.info(f"[JSON-1] Найден JSON в markdown блоке для клиента {conv_id}")
            result = self._try_parse_json(json_str, "markdown", conv_id)
            if result:
                return result
        
        # Стратегия 2: Поиск JSON по фигурным скобкам
        json_start = response_text.find('{')
        json_end = response_text.rfind('}') + 1
        if json_start != -1 and json_end > json_start:
            json_str = response_text[json_start:json_end].strip()
            logging.info(f"[JSON-2] Найден JSON в тексте {json_start}-{json_end} для клиента {conv_id}")
            result = self._try_parse_json(json_str, "brackets", conv_id)
            if result:
                return result
        
        # Стратегия 3: Поиск JSON через регулярные выражения (более агрессивный подход)
        json_patterns = [
            r'\{[\s\S]*?\}',  # Любой текст между { и }
            r'"[^"]*":\s*\{[\s\S]*?\}',  # Объект с ключом
            r'\{[\s\S]*?"[^"]*"[\s\S]*?\}',  # Объект с ключами
        ]
        
        for i, pattern in enumerate(json_patterns):
            matches = re.findall(pattern, response_text, re.MULTILINE)
            for j, match in enumerate(matches):
                logging.info(f"[JSON-3.{i}.{j}] Пробуем паттерн {i+1}, вариант {j+1} для клиента {conv_id}")
                result = self._try_parse_json(match.strip(), f"pattern_{i}_{j}", conv_id)
                if result:
                    return result
        
        # Стратегия 4: Очистка от мусорных символов
        cleaned_text = re.sub(r'^[^{]*', '', response_text)  # Убираем всё до первой {
        cleaned_text = re.sub(r'[^}]*$', '', cleaned_text)   # Убираем всё после последней }
        if cleaned_text:
            logging.info(f"[JSON-4] Очищенный текст для клиента {conv_id}")
            result = self._try_parse_json(cleaned_text, "cleaned", conv_id)
            if result:
                return result
        
        # Если ничего не получилось - логируем детали и возвращаем fallback
        logging.error(f"[JSON-FAIL] НЕ УДАЛОСЬ извлечь JSON для клиента {conv_id}")
        logging.error(f"[JSON-FAIL] Первые 500 символов ответа: {response_text[:500]}")
        logging.error(f"[JSON-FAIL] Последние 200 символов ответа: {response_text[-200:]}")
        
        raise ValueError(f"Не удалось извлечь валидный JSON из ответа ИИ для клиента {conv_id}")
    
    def _try_parse_json(self, json_str: str, method: str, conv_id: str) -> Optional[Dict[str, Any]]:
        """Попытка парсинга JSON с различными методами очистки"""
        attempts = [
            json_str,  # Как есть
            json_str.strip(),  # Убираем пробелы
            json_str.replace('\n', '').replace('\r', '').strip(),  # Убираем переносы
            json_str.replace('\\"', '"'),  # Убираем экранирование кавычек
            re.sub(r'^"', '', re.sub(r'"$', '', json_str.strip())),  # Убираем внешние кавычки
            re.sub(r'[\x00-\x1f\x7f-\x9f]', '', json_str),  # Убираем управляющие символы
        ]
        
        for i, attempt in enumerate(attempts):
            try:
                result = json.loads(attempt)
                logging.info(f"[JSON-OK] Успешный парсинг методом {method}, попытка {i+1} для клиента {conv_id}")
                return result
            except json.JSONDecodeError as e:
                logging.debug(f"[JSON-TRY] Метод {method}, попытка {i+1} провалилась для клиента {conv_id}: {e}")
                continue
            except Exception as e:
                logging.debug(f"[JSON-ERR] Метод {method}, попытка {i+1}, неожиданная ошибка для клиента {conv_id}: {e}")
                continue
        
        return None
    
    def _create_fallback_analysis(self, client_data: Dict[str, Any]) -> Dict[str, Any]:
        """Создание базового анализа в случае сбоя ИИ (чтобы скрипт НЕ упал)"""
        conv_id = client_data.get('conv_id', 'UNKNOWN')
        existing_profile = client_data.get('existing_profile', {})
        
        logging.warning(f"[FALLBACK] Создаём базовый анализ для клиента {conv_id}")
        
        # Базовая структура, которая не сломает обработку
        fallback_result = {
            "profile_updates": {
                "lead_qualification": existing_profile.get('lead_qualification', ['холодный_клиент']),
                "funnel_stage": existing_profile.get('funnel_stage', 'первый_контакт'),
                "client_level": existing_profile.get('client_level', ['начинающий']),
                "learning_goals": existing_profile.get('learning_goals', ['не_определены']),
                "client_pains": existing_profile.get('client_pains', ['не_определены']),
                "email": existing_profile.get('email', []),
                "client_activity": existing_profile.get('client_activity', 'низкая')
            },
            "strategic_analysis": {
                "short_term_strategy": "Требуется повторный анализ - ИИ не смог обработать карточку клиента",
                "long_term_strategy": "Мануальная проверка консультантом из-за технических проблем анализа",
                "key_focus_areas": ["технический_анализ_провален"]
            },
            "mandatory_reminder": {
                "contact_in_days": 1,
                "optimal_utc_hour": 12,
                "contact_reason": "Техническая проблема анализа ИИ - требуется мануальная проверка",
                "reminder_context": f"КРИТИЧНО: ИИ не смог проанализировать клиента {conv_id}. Требуется мануальная обработка."
            },
            "analysis_metadata": {
                "analysis_status": "fallback_analysis",
                "analysis_timestamp": datetime.now(timezone.utc).isoformat(),
                "fallback_reason": "json_parsing_failed"
            }
        }
        
        return fallback_result
    
    def update_client_profile(self, conv_id: int, analysis_result: Dict[str, Any]) -> bool:
        """Обновление профиля клиента в БД на основе анализа"""
        try:
            logging.info(f"[DB_UPDATE] Начинаем обновление профиля клиента {conv_id}")
            logging.info(f"[DB_UPDATE] Полученные данные анализа:")
            logging.info(f"{json.dumps(analysis_result, ensure_ascii=False, indent=2, default=str)}")
            
            with self.get_db_connection() as conn:
                with conn.cursor() as cur:
                    strategic_data = analysis_result.get('strategic_analysis', {})
                    profile_updates = analysis_result.get('profile_updates', {})
                    
                    logging.info(f"[DB_UPDATE] Извлеченные strategic_data: {strategic_data}")
                    logging.info(f"[DB_UPDATE] Извлеченные profile_updates: {profile_updates}")
                    
                    # Валидация и очистка данных
                    def clean_qualification(qual_list):
                        """Очищает и валидирует квалификацию клиента"""
                        valid_qualifications = ['горячий', 'тёплый', 'холодный', 'клиент', 'не определено']
                        if isinstance(qual_list, list) and qual_list:
                            # Берём первое значение и очищаем его
                            qual = str(qual_list[0]).replace('_клиент', '').strip()
                            if qual in valid_qualifications:
                                return [qual]  # Возвращаем как массив
                        return ['не определено']  # Возвращаем как массив
                    
                    def clean_funnel_stage(stage):
                        """Очищает и валидирует этап воронки"""
                        valid_stages = [
                            'не применимо',
                            'предложение по продуктам ещё не сделано',
                            'сделано предложение по продуктам',
                            'сделано новое предложение',
                            'клиент думает',
                            'у клиента есть возражения',
                            'отказ от покупки',
                            'решение принято (ожидаем оплату)',
                            'покупка совершена'
                        ]
                        if isinstance(stage, str) and stage in valid_stages:
                            return stage
                        return 'не определено'
                    
                    def clean_array_field(field_data):
                        """Очищает массивы от лишних символов"""
                        if isinstance(field_data, list):
                            return field_data
                        elif isinstance(field_data, str):
                            # Убираем фигурные скобки и парсим как массив
                            cleaned = field_data.strip('{}[]"').split(',')
                            return [item.strip().strip('"') for item in cleaned if item.strip()]
                        return []
                    
                    # Очищаем данные
                    clean_qualification_value = clean_qualification(profile_updates.get('lead_qualification', []))
                    clean_funnel_stage_value = clean_funnel_stage(profile_updates.get('funnel_stage', ''))
                    clean_client_level = clean_array_field(profile_updates.get('client_level', []))
                    clean_learning_goals = clean_array_field(profile_updates.get('learning_goals', []))
                    clean_client_pains = clean_array_field(profile_updates.get('client_pains', []))
                    clean_email = clean_array_field(profile_updates.get('email', []))
                    
                    update_data = {
                        'lead_qualification': clean_qualification_value,  # Уже массив
                        'funnel_stage': clean_funnel_stage_value,
                        'client_level': clean_client_level,
                        'learning_goals': clean_learning_goals,
                        'client_pains': clean_client_pains,
                        'email': clean_email,
                        'client_activity': profile_updates.get('client_activity', ''),
                        'short_term_strategy': strategic_data.get('short_term_strategy', ''),
                        'long_term_strategy': strategic_data.get('long_term_strategy', ''),
                        'last_strategy_analysis': datetime.now(timezone.utc),
                        'strategy_analysis_data': json.dumps(analysis_result, ensure_ascii=False)
                    }
                    
                    logging.info(f"[DB_UPDATE] Подготовленные ОЧИЩЕННЫЕ данные для обновления:")
                    for key, value in update_data.items():
                        logging.info(f"  {key}: {value} (тип: {type(value)})")
                    
                    # Проверяем, существует ли профиль
                    cur.execute("SELECT conv_id FROM user_profiles WHERE conv_id = %s", (conv_id,))
                    exists = cur.fetchone()
                    
                    if exists:
                        logging.info(f"[DB_UPDATE] Профиль {conv_id} существует, обновляем")
                        # Обновляем существующий профиль
                        sql_query = """
                            UPDATE user_profiles SET
                                lead_qualification = %s,
                                funnel_stage = %s,
                                client_level = %s,
                                learning_goals = %s,
                                client_pains = %s,
                                email = %s,
                                client_activity = %s,
                                short_term_strategy = %s,
                                long_term_strategy = %s,
                                last_strategy_analysis = %s,
                                strategy_analysis_data = %s,
                                last_updated = %s
                            WHERE conv_id = %s
                        """
                        
                        sql_params = (
                            update_data['lead_qualification'],
                            update_data['funnel_stage'],
                            update_data['client_level'],
                            update_data['learning_goals'],
                            update_data['client_pains'],
                            update_data['email'],
                            update_data['client_activity'],
                            update_data['short_term_strategy'],
                            update_data['long_term_strategy'],
                            update_data['last_strategy_analysis'],
                            update_data['strategy_analysis_data'],
                            datetime.now(timezone.utc),
                            conv_id
                        )
                        
                        logging.info(f"[DB_UPDATE] Выполняем SQL: {sql_query}")
                        logging.info(f"[DB_UPDATE] ===== ПОЛНЫЕ ПАРАМЕТРЫ SQL ===== conv_id:{conv_id} =====")
                        for i, param in enumerate(sql_params):
                            logging.info(f"  Параметр {i+1}: {param} (тип: {type(param)})")
                        logging.info(f"[DB_UPDATE] ===== КОНЕЦ ПАРАМЕТРОВ SQL ===== conv_id:{conv_id} =====")
                        
                        cur.execute(sql_query, sql_params)
                        
                        affected_rows = cur.rowcount
                        if affected_rows > 0:
                            logging.info(f"Профиль клиента {conv_id} обновлён (включая стратегические столбцы)")
                            conn.commit()
                            return True
                        else:
                            logging.warning(f"Профиль клиента {conv_id} не обновлён")
                            return False
                    else:
                        logging.warning(f"Профиль клиента {conv_id} не найден, пропускаем обновление")
                        return False
                    
        except Exception as e:
            logging.error(f"Ошибка обновления профиля клиента {conv_id}: {e}")
            return False
    
    def create_strategic_reminder(self, conv_id: int, analysis_result: Dict[str, Any]) -> bool:
        """Создание обязательного стратегического напоминания с распределением по минутам"""
        try:
            reminder_data = analysis_result.get('mandatory_reminder', {})
            
            if not reminder_data:
                logging.warning(f"Данные mandatory_reminder отсутствуют для клиента {conv_id}")
                return False
            
            contact_in_days = reminder_data.get('contact_in_days', 1)
            optimal_utc_hour = reminder_data.get('optimal_utc_hour', 12)
            reminder_context = reminder_data.get('reminder_context', 'Стратегическое взаимодействие')
            
            with self.get_db_connection() as conn:
                with conn.cursor() as cur:
                    # Находим свободное время (макс 1 напоминание в минуту)
                    target_date = datetime.now(timezone.utc).date() + timedelta(days=contact_in_days)
                    base_time = datetime.combine(target_date, datetime.min.time()).replace(tzinfo=timezone.utc, hour=optimal_utc_hour)
                    
                    # Проверяем свободные минуты в течение часа
                    for minute_offset in range(60):
                        candidate_time = base_time + timedelta(minutes=minute_offset)
                        
                        # Проверяем, свободна ли эта минута
                        cur.execute("""
                            SELECT COUNT(*) FROM reminders 
                            WHERE status = 'active'
                            AND reminder_datetime BETWEEN %s AND %s
                        """, (
                            candidate_time,
                            candidate_time + timedelta(seconds=59)
                        ))
                        
                        result = cur.fetchone()
                        if result and result[0] == 0:  # Минута свободна
                            reminder_datetime = candidate_time
                            break
                    else:
                        # Если в текущем часу нет места, переносим на следующий час
                        reminder_datetime = base_time + timedelta(hours=1)
                        logging.warning(f"Час {optimal_utc_hour}:00 занят, переносим на {optimal_utc_hour+1}:00")
                    
                    # Создаем напоминание
                    cur.execute("""
                        INSERT INTO reminders (
                            conv_id, reminder_datetime, reminder_context_summary,
                            created_by_conv_id, client_timezone, status
                        ) VALUES (%s, %s, %s, %s, %s, 'active')
                        RETURNING id
                    """, (
                        conv_id, reminder_datetime, reminder_context, None, 'UTC'
                    ))
                    
                    result = cur.fetchone()
                    if result:
                        reminder_id = result[0]
                        conn.commit()
                        logging.info(f"✅ Напоминание ID={reminder_id} для {conv_id} на {reminder_datetime}")
                        return True
                    else:
                        logging.error(f"Ошибка: не удалось создать напоминание")
                        return False
                    
        except Exception as e:
            logging.error(f"Ошибка создания напоминания: {e}")
            return False

    def create_reminder_if_needed(self, conv_id: int, analysis_result: Dict[str, Any]) -> bool:
        """Создание напоминания на основе анализа (старый метод для совместимости)"""
        try:
            optimal_timing = analysis_result.get('optimal_reminder_timing', {})
            return_strategies = analysis_result.get('return_strategies', {})
            
            # Проверяем, нужно ли создавать напоминание
            contact_in_days = optimal_timing.get('contact_in_days', 0)
            needs_reengagement = analysis_result.get('conversation_gaps', {}).get('needs_reengagement', False)
            
            if not needs_reengagement or contact_in_days <= 0:
                logging.info(f"Напоминание для клиента {conv_id} не требуется")
                return True
            
            # Вычисляем время напоминания
            reminder_datetime = datetime.now(timezone.utc) + timedelta(days=contact_in_days)
            
            # Формируем контекст напоминания
            contact_reason = optimal_timing.get('contact_reason', 'Возобновление диалога')
            recommended_approach = return_strategies.get('recommended_approach', 'Мягкое напоминание')
            
            reminder_context = f"{contact_reason}. Стратегия: {recommended_approach}"
            
            with self.get_db_connection() as conn:
                with conn.cursor() as cur:
                    # Проверяем, нет ли уже активного напоминания на ближайшее время
                    cur.execute("""
                        SELECT id FROM reminders 
                        WHERE conv_id = %s AND status = 'active'
                        AND reminder_datetime BETWEEN %s AND %s
                    """, (
                        conv_id,
                        reminder_datetime - timedelta(hours=12),
                        reminder_datetime + timedelta(hours=12)
                    ))
                    
                    existing_reminder = cur.fetchone()
                    if existing_reminder:
                        logging.info(f"Напоминание для клиента {conv_id} уже существует")
                        return True
                    
                    # Создаем новое напоминание
                    cur.execute("""
                        INSERT INTO reminders (
                            conv_id, reminder_datetime, reminder_context_summary,
                            created_by_conv_id, client_timezone, status
                        ) VALUES (%s, %s, %s, %s, %s, 'active')
                        RETURNING id
                    """, (
                        conv_id,
                        reminder_datetime,
                        reminder_context,
                        None,  # Создано автоматически анализатором
                        'Europe/Moscow'  # По умолчанию московское время
                    ))
                    
                    result = cur.fetchone()
                    if result:
                        reminder_id = result[0]
                        conn.commit()
                        logging.info(f"Создано напоминание ID={reminder_id} для клиента {conv_id} на {reminder_datetime}")
                        return True
                    else:
                        logging.error(f"Ошибка: не удалось создать напоминание")
                        return False
                    
        except Exception as e:
            logging.error(f"Ошибка создания напоминания для клиента {conv_id}: {e}")
            return False
    
    def save_analysis_result(self, conv_id: int, analysis_result: Dict[str, Any]) -> bool:
        """Сохранение результата анализа в файл"""
        try:
            # Создаем директорию для результатов, если её нет
            results_dir = "analysis_results"
            os.makedirs(results_dir, exist_ok=True)
            
            # Формируем имя файла с временной меткой
            timestamp = datetime.now().strftime("%Y%m%d_%H%M%S")
            filename = f"client_{conv_id}_analysis_{timestamp}.json"
            filepath = os.path.join(results_dir, filename)
            
            # Добавляем метаданные
            result_with_meta = {
                "conv_id": conv_id,
                "analysis_timestamp": datetime.now(timezone.utc).isoformat(),
                "analyzer_version": "2.0",
                "analysis_result": analysis_result
            }
            
            # Сохраняем в файл
            with open(filepath, 'w', encoding='utf-8') as f:
                json.dump(result_with_meta, f, ensure_ascii=False, indent=2, default=str)
            
            logging.info(f"Результат анализа сохранен в {filepath}")
            return True
            
        except Exception as e:
            logging.error(f"Ошибка сохранения результата анализа: {e}")
            return False
    
    def load_from_json(self, json_file_path: str) -> Dict[str, Any]:
        """Загрузка данных клиента из JSON файла"""
        try:
            with open(json_file_path, 'r', encoding='utf-8') as f:
                client_data = json.load(f)
            
            logging.info(f"Данные клиента загружены из файла {json_file_path}")
            return client_data
            
        except Exception as e:
            logging.error(f"Ошибка загрузки данных из JSON: {e}")
            raise

    def analyze_client(self, conv_id: int, save_to_file: bool = True) -> Dict[str, Any]:
        """Полный анализ клиента с обновлением БД"""
        try:
            logging.info(f"Начинаем полный анализ клиента {conv_id}")
            
            # 1. Загружаем данные клиента из БД
            client_data = self.load_client_data_from_db(conv_id)
            
            # 2. Проводим анализ с помощью AI
            analysis_result = self.analyze_client_card(client_data)
            
            # 3. Обновляем профиль клиента в БД
            profile_updated = self.update_client_profile(conv_id, analysis_result)
            if not profile_updated:
                logging.warning(f"Не удалось обновить профиль клиента {conv_id}")
            
            # 4. Создаем напоминание, если необходимо
            reminder_created = self.create_reminder_if_needed(conv_id, analysis_result)
            if not reminder_created:
                logging.warning(f"Не удалось создать напоминание для клиента {conv_id}")
            
            # 5. Сохраняем результат в файл (опционально)
            if save_to_file:
                self.save_analysis_result(conv_id, analysis_result)
            
            # Добавляем информацию о выполненных действиях
            analysis_result["_metadata"] = {
                "profile_updated": profile_updated,
                "reminder_created": reminder_created,
                "analysis_timestamp": datetime.now(timezone.utc).isoformat()
            }
            
            logging.info(f"Полный анализ клиента {conv_id} завершен успешно")
            return analysis_result
            
        except Exception as e:
            logging.error(f"Ошибка полного анализа клиента {conv_id}: {e}")
            raise

def main():
    """Основная функция для тестирования"""
    import sys
    
    try:
        # Инициализация анализатора
        analyzer = ClientCardAnalyzer()
        
        if len(sys.argv) > 1:
            # Режим работы с JSON файлом
            json_file_path = sys.argv[1]
            print(f"Анализируем клиента из файла: {json_file_path}")
            
            # Загружаем данные из JSON
            client_data = analyzer.load_from_json(json_file_path)
            client_id = client_data.get('client_id')
            
            print(f"Запускаем AI анализ клиента {client_id}...")
            
            # Проводим анализ
            analysis_result = analyzer.analyze_client_card(client_data)
            
            # Сохраняем результат
            timestamp = datetime.now().strftime("%Y%m%d_%H%M%S")
            result_file = f"analysis_results/strategic_analysis_{client_id}_{timestamp}.json"
            
            os.makedirs("analysis_results", exist_ok=True)
            with open(result_file, 'w', encoding='utf-8') as f:
                json.dump(analysis_result, f, ensure_ascii=False, indent=2, default=str)
            
            print(f"Анализ карточки клиента завершен. Результат сохранен в: {result_file}")
            
            print("\n=== РЕЗУЛЬТАТ АНАЛИЗА ===")
            print(json.dumps(analysis_result, ensure_ascii=False, indent=2, default=str))
            
            return result_file
        else:
            # Старый режим работы с conv_id из БД
            test_conv_id = 123456789
            print(f"Анализируем клиента {test_conv_id}...")
            result = analyzer.analyze_client(test_conv_id)
            
            print("\n=== РЕЗУЛЬТАТ АНАЛИЗА ===")
            print(json.dumps(result, ensure_ascii=False, indent=2, default=str))
        
    except Exception as e:
        logging.error(f"Ошибка в main: {e}")
        print(f"Ошибка: {e}")
        return None

if __name__ == "__main__":
    main()